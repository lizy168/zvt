---
description: 本地联调与运行方式：以后涉及「如何运行项目 / 启动前后端」一律参考 docs/DEV_RUN.md
alwaysApply: true
---

# 项目运行方式（参考 docs/DEV_RUN.md）

涉及**如何运行、启动前后端、联调**时，一律按 **`docs/DEV_RUN.md`** 执行，不要用 README 或其它文档里的启动命令替代。

## 要点摘要

- **后端**：项目根目录执行，使用 `py312` 虚拟环境与 `PYTHONPATH=src`；端口 8090，`--reload`。
- **前端**：在 `zvt_ui/` 下执行，先 `ulimit -n 10240` 再 `./node_modules/.bin/next dev` 或 `./dev.sh`；端口 3000。
- **顺序**：先启后端，再启前端；前端 `.env` 中 `NEXT_PUBLIC_SERVER=http://127.0.0.1:8090`。

具体命令、环境要求、常见问题见 **`docs/DEV_RUN.md`**。若文档中后端入口为 `zvt_vip` 而当前仓库无此包，则以仓库内实际入口为准（如 `zvt.zvt_server:app`），其余步骤仍按该文档。
